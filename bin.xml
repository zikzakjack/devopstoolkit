<assembly
	xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">
	<id>bin</id>
	<formats>
		<format>tar.gz</format>
	</formats>
	<includeBaseDirectory>false</includeBaseDirectory>

	<!-- Adds dependencies under lib directory -->
	<dependencySets>
		<dependencySet>
			<useProjectArtifact>false</useProjectArtifact>
			<outputDirectory>lib</outputDirectory>
			<unpack>false</unpack>
		</dependencySet>
	</dependencySets>

	<fileSets>
		<fileSet>
			<directory>${project.basedir}/src/main/bin</directory>
			<outputDirectory>/bin</outputDirectory>
			<includes>
				<include>*</include>
			</includes>
			<fileMode>0755</fileMode>
			<filtered>true</filtered>
		</fileSet>
		<fileSet>
			<directory>${project.basedir}/src/main/config</directory>
			<outputDirectory>/config</outputDirectory>
			<includes>
				<include>**/*</include>
			</includes>
			<excludes>
				<exclude>*local*</exclude>
				<exclude>myapp*.properties</exclude>
				<exclude>log4j*.xml</exclude>
				<exclude>samplepasswordsfile</exclude>
			</excludes>
		</fileSet>
		<fileSet>
			<directory>${project.basedir}/src/main/config/</directory>
			<outputDirectory>/postDeploy/</outputDirectory>
			<includes>
				<include>myapp-*.properties</include>
				<include>log4j-*.xml</include>
			</includes>
			<excludes>
				<exclude>*local*</exclude>
			</excludes>
		</fileSet>
		<fileSet>
			<directory>${project.basedir}/src/main/postDeploy/</directory>
			<outputDirectory>/postDeploy</outputDirectory>
			<includes>
				<include>**/*</include>
			</includes>
			<fileMode>0755</fileMode>
			<filtered>true</filtered>
		</fileSet>
		<fileSet>
			<directory>${project.build.directory}</directory>
			<outputDirectory>/</outputDirectory>
			<includes>
				<include>*.jar</include>
			</includes>
			<excludes>
				<exclude>*sources.jar</exclude>
			</excludes>
		</fileSet>
	</fileSets>
</assembly>
